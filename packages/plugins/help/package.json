{
  "name": "@raulanator/utilsh-plugin-help",
  "version": "1.0.0",
  "description": "Help plugin for utilsh CLI",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write \"**/*.{ts,json}\"",
    "format:check": "prettier --check \"**/*.{ts,json}\""
  },
  "dependencies": {
    "@raulanator/utilsh-core": "^1.0.0",
    "commander": "^12.0.0"
  }
}
