{
  "name": "@raulanatol/utilsh-plugin-hello",
  "version": "1.0.0",
  "description": "Hello plugin for utilsh CLI",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write \"**/*.{ts,json}\"",
    "format:check": "prettier --check \"**/*.{ts,json}\""
  },
  "dependencies": {
    "@raulanatol/utilsh-core": "^1.0.0",
    "commander": "^12.0.0"
  }
}
